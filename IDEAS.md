# 추가하면 좋을 것들 (제안 목록)

작성일: 2026-07-25
기준: `plan.md` v0.3.9(Phase 0~4 완료 시점), `README.md`, `backend/`, `phase1/` 코드 훑어본 결과

우선순위나 일정 확정 없이 아이디어를 최대한 넓게 모은 문서. 실제로 할지는 나중에 골라서 `plan.md`에 반영.

---

## 1. plan.md에 이미 있는 Phase 5 항목 (참고용 — 새 아이디어 아님)

여기 적힌 나머지 항목과 헷갈리지 않도록 먼저 정리:

- 허밍/음성 입력 (P2)
- AI 보컬/가창 생성
- 실제 악기 마이크 입력
- 곡 공유
- 모바일/터치 최적화
- 소셜 로그인

아래부터는 이 목록에 없는 새 제안들.

---

## 2. 테스트 & 안정성

- **백엔드 테스트 전무**: `accounts/tests.py`, `projects/tests.py`가 둘 다 3줄짜리 Django 기본 보일러플레이트(실제 테스트 0개). `community` 앱은 테스트 파일 자체가 없음. 회원가입/로그인, 프로젝트 CRUD 권한(본인 것만 접근), WebSocket 협업 동시편집 최소한 이 세 곳은 회귀 테스트 있으면 좋음
- **프론트엔드 테스트 전무**: `phase1`에 `*.test.*`/`*.spec.*` 파일 자체가 없음. 키 검출(Krumhansl-Schmuckler), 퀀타이즈, WAV 인코딩처럼 순수 로직 함수는 Vitest로 유닛 테스트 붙이기 좋은 대상
- **CI 파이프라인 없음**: `.github/workflows` 없음. PR마다 최소 `pytest` + `npm run lint` + `npm run build` 정도만 돌아도 회귀를 미리 잡음
- **에러 트래킹 없음**: Sentry 같은 것 연동 안 되어 있음. 배포된 상태(Render)라 실제 유저가 에러 만나도 로그로만 남고 알림 못 받음. 무료 티어로도 충분히 붙일 수 있음

## 3. 백엔드 확장성/보안

- **Channel Layer가 InMemoryChannelLayer** (`backend/config/settings.py:87`): 워커 1개 전제. 지금은 개인 프로젝트 규모라 문제없지만, Render에서 워커/인스턴스 늘리는 순간 실시간 협업이 인스턴스 간 브로드캐스트 안 돼서 깨짐. 나중에 `channels_redis`로 교체할 지점이라고 주석에도 이미 적혀 있음 — 실제로 트래픽 늘 때 우선순위 1번
- **DRF Throttle 설정 없음**: 로그인/회원가입 엔드포인트에 무제한 요청 가능. `rest_framework.throttling`으로 로그인 시도 제한 정도는 붙여둘 만함 (무차별 대입 방지)
- **API 문서화 없음**: Swagger/OpenAPI(`drf-spectacular` 등) 붙이면 프론트-백엔드 계약이 문서로 남고, 포트폴리오로 보여주기도 좋음

## 4. README/문서 정리

- **README가 plan.md보다 뒤처짐**: README "아직 없는 것" 항목에 WAV 내보내기가 남아있는데, `plan.md`는 v0.3.9에서 WAV 내보내기 완료로 기록됨. AI 코멘트도 v0.3.7에 이미 구현 완료라고 나와 있음. README 갱신 필요
- **`.env.example` 최신 상태 확인**: 커뮤니티 기능(Ollama LLM 연동, WebSocket 채팅) 추가되면서 필요해진 환경변수가 `.env.example`에 다 반영됐는지 점검 필요

## 5. 음악/오디오 기능 아이디어 (plan.md Phase 5에 없는 것들)

- **템포/BPM 변경 시 기존 녹음 자동 리샘플링**: 지금은 BPM을 녹음 시작 시 고정하는 구조로 보이는데, 나중에 BPM을 바꾸고 싶을 때 노트 타이밍을 비율로 재계산해주는 기능
- **트랙 볼륨 자동화(오토메이션)**: 지금은 트랙 전체 볼륨만 조절 가능한 걸로 보임. 구간별로 볼륨이 서서히 변하는 페이드 인/아웃 정도는 초보자도 쓸만한 기능
- **코드 진행 프리셋 다양화**: 현재 다이어토닉 진행 템플릿(예: I–V–vi–IV) 하나 방식인데, 프리셋 여러 개 두고 사용자가 골라볼 수 있게 하면 "AI가 만든 반주가 다 똑같다"는 인상을 줄일 수 있음
- **되돌리기/다시하기(Undo/Redo)**: 미니 DAW에서 블록 이동/자르기/삭제, 퀀타이즈 등 편집 동작에 대한 undo 스택. 실수로 지운 블록 복구할 방법이 지금 퀀타이즈 원본 보존 말고는 없어 보임
- **곡 썸네일/파형 미리보기**: 프로젝트 목록 화면에서 곡을 파형 이미지나 짧은 재생 프리뷰로 구분 가능하게

## 6. 협업/커뮤니티 기능 확장

- **동시편집 충돌 처리**: 두 사람이 같은 블록을 동시에 편집하면 어떻게 되는지(마지막 쓰기 승리? 잠금?) 명시 안 되어 있으면 정의해두면 좋음
- **접속자 프레즌스 완전화**: README에 이미 "나중에 합류한 사람만 반영되는 흠"으로 적혀 있음 — 소소하지만 고칠 만한 버그
- **버전 히스토리**: 프로젝트 저장할 때마다 스냅샷을 남겨서 이전 버전으로 되돌릴 수 있게 (특히 협업 중 누가 잘못 편집했을 때 유용)
- **댓글/피드백**: 공개 프로젝트에 다른 유저가 댓글 남기는 기능 (곡 공유 기능과 세트로 묶이면 자연스러움)

## 7. 배포/운영

- **관리자 알림**: `community` 앱에 건의하기(Suggestion) 기능이 있는데, 새 건의 들어와도 관리자가 admin에 직접 들어가야 앎. 이메일/슬랙 알림 정도 붙이면 운영 편해짐
- **DB 백업 정책**: PostgreSQL 데이터 백업이 Render 무료 티어에서 자동으로 되는지 확인, 안 되면 최소 수동 백업 스크립트 하나
- ~~**Ollama 서빙 방식 재검토**~~ → 해결(v0.3.10): Render 무료 티어는 8B 모델 자체 서빙 불가 확인, Groq API로 교체 완료

## 8. 코드 품질

- **TypeScript 미도입 확정 상태**: `plan.md`에 JSDoc으로 보완한다고 명시돼 있음. 지금 규모에서 문제없어 보이나, 프로젝트 커지면 재검토 대상으로 남겨두기
- **프론트 오류 바운더리(Error Boundary)**: React 컴포넌트 하나가 죽어도 스튜디오 화면 전체가 하얗게 안 되도록 최소한의 ErrorBoundary
