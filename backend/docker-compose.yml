services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: music_studio
      POSTGRES_USER: music_studio
      POSTGRES_PASSWORD: music_studio
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
