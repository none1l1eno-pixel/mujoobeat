# 🎵 AI 합주 스튜디오 (가칭) — 기획서 v0.3.9

작성일: 2026-07-21 | 버전: v0.3.9 | 상태: Phase 4(WAV 내보내기) 완료 — MVP(Phase 0~4) 전 항목 구현 완료

> **v0.3.8 → v0.3.9 변경 요약**: **Phase 4 마지막 항목 WAV 내보내기 구현** — `Tone.Offline`로 편곡 전체를 실시간보다 빠르게 오프라인 렌더링한 뒤 16bit PCM WAV로 인코딩해 브라우저에서 바로 다운로드(`audio/exportWav.js`). 서버 왕복 없이 전부 클라이언트에서 처리(9.1절 "소리는 브라우저" 원칙 유지). 스튜디오 상단 바에 "WAV 내보내기" 버튼 추가. 구현 중 실제 버그 하나 발견·수정: Magenta 드럼 생성이 같은 스텝에 서로 다른 피치를 둘 다 하이햇류로 매핑해 **같은 시각에 같은 드럼 신스를 두 번 트리거**하는 경우가 있어("Start time must be strictly greater than previous start time" 크래시로 재현) `magentaDrums.js`에서 (사운드, 시작시각) 기준 중복 제거로 고침 — 오프라인 렌더링뿐 아니라 실시간 재생에서도 잠재적으로 있었을 결함. CDP로 멜로디만 있는 트랙, AI 드럼+코드 트랙 둘 다 내보내기 검증(각각 WAV blob 정상 생성, 에러 없음). **Phase 0~4(MVP 정의, 10장) 전 항목 완료** — 이제 로그인 사용자가 멜로디를 치면 AI가 반주를 붙이고, 박자를 고치고, 계정에 저장하고, WAV로 내보낼 수 있다

> **v0.3.7 → v0.3.8 변경 요약**: **전체 화면 공통 커뮤니티 위젯 3종 신설**(로그인 후 프로젝트 목록/스튜디오 어느 화면에서든 우상단·우하단에 항상 노출). (1) **공지사항**(우상단 종 아이콘): 운영자가 Django admin에서 작성한 공지를 REST(`GET /api/community/announcements/`)로 노출, 안 읽은 공지 있으면 배지 표시(localStorage에 마지막 확인 시각 저장). (2) **건의하기**(우상단 연필 아이콘): 유저가 텍스트로 건의사항 제출(`POST /api/community/suggestions/`), 운영자는 Django admin에서 확인. (3) **전체 채팅**(우하단 말풍선 아이콘, 플로팅): 방 구분 없이 로그인한 모든 유저가 하나의 채널을 공유하는 실시간 채팅 — WebSocket(`ws/chat/global/`, `projects/consumers.py`의 방식과 동일하게 JWT 인증) + 이력은 REST(`GET /api/community/chat/history/`, 최근 50개)로 최초 로드. 닫혀있을 때 새 메시지 오면 배지 카운트. 새 Django 앱 `community` 하나로 세 기능 모두 구현(모델 3종: Announcement/Suggestion/ChatMessage). 2브라우저 실시간 브로드캐스트 및 안읽은 공지 배지 CDP 테스트로 검증 완료(에러 없음)

> **v0.3.6 → v0.3.7 변경 요약**: **AI 엔진을 규칙 기반에서 실제 ML/LLM 모델로 교체**. (1) 드럼 생성: Magenta.js(DrumsRNN, TF.js, 브라우저 내 실행)로 교체 — 기존 규칙 기반은 스타일이 같으면 항상 같은 패턴이 나오는 한계가 있었음. 짧은 프라임(한 박)만 주고 나머지는 RNN이 확률적으로 생성해 같은 설정이어도 매번 다름(실측: 3회 생성 시 노트 수 16/26/21로 전부 다름). 모델 로딩 실패 시 기존 규칙 기반으로 자동 폴백. 번들 크기 때문에(TF.js 포함 2.5MB) 동적 import로 분리해 실제 생성 시에만 로드. (2) AI 코멘트(P3, 신규 구현): LLaMA 3.1 8B를 로컬 Ollama로 서빙, Django가 프로젝트 데이터를 요약해 프롬프트로 전달·중계(9.1절 "LLM API 중계" 원칙 그대로). 코드는 여전히 규칙 기반 다이어토닉 진행 유지(멜로디→코드 변환에 맞는 Magenta 모델이 없어 정확성 우선)

> **v0.3.3 → v0.3.4 변경 요약**: **Phase 2(미니 DAW) 착수** — 멀티트랙 타임라인, 블록 이동/자르기/복사/삭제, 트랙별 볼륨·뮤트·솔로, 퀀타이즈(강도/그리드/미리듣기→확정·취소) 구현. 미결정이었던 "퀀타이즈 전 원본 보존 여부" **보존으로 확정**(12장)

> **v0.3.2 → v0.3.3 변경 요약**: **가상 악기 음색 종류 확정** — 피아노 하나뿐이던 선율악기를 기타/바이올린/색소폰까지 확장(5장 P1). 전부 샘플 기반 유지(신스 금지 원칙 불변), 동일 건반 UI에 옥타브 이동 추가로 좁은 음역 문제 해소

> **v0.3.1 → v0.3.2 변경 요약**: **플랫폼 우선순위를 모바일 우선으로 역전** (기존 "데스크톱 우선·모바일 P4"를 뒤집음 — 6장, 8장 갱신). 키보드 자판 연주 전제가 깨져 가상 악기는 **터치 우선 입력**으로 재설계 필요. 데스크톱은 후순위 지원 대상으로 유지

> **v0.3 → v0.3.1 변경 요약**: 사용자 계정을 **MVP 필수로 확정** (곡의 소유자 식별이 요구사항). Phase 3.5에 인증 작업 추가(+1주), 관련 미결정 해소

---

## 1. 한 줄 요약

**"악기를 몰라도, 가상 악기로 멜로디를 치면 AI가 반주와 박자를 붙여 곡을 완성해주는 웹 기반 작곡 놀이터"**

## 2. 프로젝트 목적 (확정)

**학습 및 포트폴리오용 개인 프로젝트.**
- 상업적 출시·수익화는 목표가 아님
- 학습 목표: Web Audio/Tone.js 오디오 프로그래밍, 음악 이론의 코드화(키 검출·화성 생성), **React 프론트 + Django/PostgreSQL 백엔드 풀스택 설계**
- 따라서 시장 검증보다 **"동작하는 핵심 경험을 끝까지 완성하는 것"**이 성공 기준

### 2.1 경쟁 서비스 대비 포지셔닝 (참고용)

| 서비스 | 특징 | 본 프로젝트와의 차이 |
|---|---|---|
| Suno / Udio | 텍스트·허밍 → 완성곡 통째로 생성 | 결과물은 뛰어나나 사용자가 "만드는 과정"이 없음. 본 프로젝트는 **연주→편집→보완**의 과정 자체가 경험 |
| BandLab / Soundtrap | 웹 DAW, 협업 중심 | 여전히 DAW 문법을 알아야 함. 본 프로젝트는 DAW를 몰라도 되는 것이 전제 |
| Chrome Music Lab | 음악 놀이/교육 실험 | 놀이에서 끝나고 "내 곡"이라는 결과물이 없음 |

**포지션**: "완성곡을 뽑아주는 AI"가 아니라 **"내가 연주한 것을 곡으로 키워주는 AI"**. (포트폴리오용이므로 이 차별화는 검증 대상이 아닌 설계 방향으로만 사용)

---

## 3. 타겟 사용자 (v0.2 수정)

**주 페르소나**: "게임하듯 가상 악기를 두드리며 놀 수는 있지만, 그걸 곡으로 만드는 법은 모르는 사람"
- 악기 실물은 못 다뤄도 화면 피아노/드럼패드는 눌러볼 수 있는 일반인
- 음악 이론 지식 없음을 전제

> v0.1의 "멜로디는 머릿속에 있는데 곡으로 못 만드는 사람"은 자연 입력 수단이 허밍이라 P2(허밍 입력)와 우선순위가 충돌했음. v0.2에서는 페르소나를 가상 악기 사용자로 조정하고, 허밍 페르소나는 P2 출시 시점에 확장.

---

## 4. 핵심 경험과 성공 기준 (v0.2 신설)

**핵심 경험**: 멜로디 입력에서 반주 붙은 곡까지 **3분 이내** 도달.

**"그럴듯한 곡"의 테스트 가능한 정의**:
1. **블라인드 테스트**: 비음악인 5명에게 결과물을 들려줬을 때 3명 이상이 "곡처럼 들린다"고 답한다
2. **기술 기준**: 생성된 반주가 (a) 멜로디와 같은 키의 다이어토닉 코드로 구성되고 (b) 드럼이 멜로디 템포와 일치하며 (c) 최소 2개 섹션(예: 벌스 패턴/필인)의 변화를 가진다
3. **부정 기준**: "미디 벨소리 같다"는 평가가 5명 중 3명 이상이면 실패 → 사운드 폰트/음색 교체 우선 대응

이 기준은 **Phase 0에서 최초 측정**하고, 이후 Phase마다 재측정한다.

**표본 규칙 (v0.2.1 확정)**: 가족/지인 중 비음악인 **평가 풀 8명**을 미리 확보한다. 회차별 평가단은 **고정 3명 + 풀에서 로테이션 2명 = 5명**. 고정 3명은 회차 간 비교의 기준선 역할, 로테이션 2명은 학습 효과(귀가 익숙해져 후해지는 평가) 보정 역할. 풀 8명 확보가 어려우면 최소 6명까지 허용하되 규칙은 동일하게 유지.

---

## 5. 핵심 기능 (우선순위 순)

### ⭐ P0 — AI 반주/박자 생성 (서비스의 심장)

1. **박자/드럼 생성**: 멜로디의 노트 밀도·BPM 기반 장르별 드럼 패턴 매칭 + 필인 삽입
2. **코드/반주 생성**: 키 검출 → 다이어토닉 코드 진행 템플릿(예: I–V–vi–IV) 매칭 → 피아노 보이싱 생성
3. **커스텀 박자 편집**: 16스텝 드럼 시퀀서에서 AI 결과를 직접 수정
4. **스타일 프리셋**: 팝 / 락 / 발라드 / 로파이 (패턴·음색 세트 교체)

### ⭐ P1 — 가상 악기 연주 + 트랙 편집

- **가상 악기 (주 입력)**: 화면 피아노, 드럼 패드. 터치 우선(모바일) + 키보드 자판/마우스(데스크톱 보조)
  - **악기 음색 종류 (v0.3.3 확정)**: 피아노 / 기타 / 바이올린 / 색소폰(선율악기, 동일 건반 UI + 옥타브 이동) + 드럼(패드). 전부 샘플 기반(9.2절 "신스 음색 금지" 원칙 유지)
- **미니 DAW**: 멀티트랙 타임라인, 블록 이동/자르기/복사/삭제, 트랙별 볼륨·음소거·솔로
- **퀀타이즈 (설계 주의)**: 양날의 검임을 전제로 설계
  - 강도 조절 지원 (50% / 100%)
  - 적용 전 **미리듣기 → 확정/취소** 플로우 필수
  - 그리드 단위 선택 (1/8, 1/16)
- **합주 사운드**: 여러 악기 트랙을 겹쳐 혼자서 밴드 사운드 (실시간 멀티플레이 아님)

### P2 — 허밍/음성 입력

- 마이크 허밍 → 피치 감지 → 미디 노트 변환 → 사용자가 노트 수정 가능한 검수 UI → AI 반주 적용
- 이 시점에 페르소나를 "허밍만 할 줄 아는 사람"까지 확장

### P3 — AI 평가 (✅ C안으로 확정)

- **격려형 코멘트 방식 확정**: 점수 없이 "잘한 점 1~2개 + 구체적 개선 팁 1~2개"
- 구현: 곡 데이터(키, 템포, 트랙 구성, 노트 통계)를 텍스트로 요약 → LLM API에 전달 → 코멘트 생성
- 예시: "후렴 멜로디의 반복 구조가 좋아요! 두 번째 벌스에서 드럼 하이햇을 절반으로 줄이면 후렴이 더 터질 거예요."

### P4 (2차 목표)

- AI 보컬/가창 생성, 실제 악기 마이크 입력, 곡 공유, **모바일/터치 최적화**

---

## 6. 입력 제약 및 지원 범위 (v0.3.2 갱신)

| 항목 | 결정 | 이유 |
|---|---|---|
| **녹음 방식** | **메트로놈 필수** (BPM을 먼저 정하고 카운트인 후 녹음) | 자유 연주의 템포 추정은 신뢰도가 낮음. BPM을 시스템이 알고 시작하면 템포 추정 문제 자체가 사라지고 퀀타이즈·드럼 생성 정확도가 크게 상승 |
| **플랫폼** (v0.3.2 역전) | **모바일 브라우저 우선**, 데스크톱은 후순위 지원 | 주 사용 맥락이 폰. 가상 악기 입력은 **터치 우선**(키보드 자판 연주는 데스크톱 보조 수단으로 격하). 레이아웃은 모바일 뷰포트 기준으로 먼저 설계하고 데스크톱은 반응형으로 확장 |
| **키 검출** | Krumhansl-Schmuckler 알고리즘 기반, 검출 결과를 사용자에게 표시하고 **수동 변경 허용** | 알고리즘 오검출 대비 탈출구 제공 |

---

## 7. 사용자 시나리오 (핵심 플로우)

```
[시작] 새 프로젝트 → BPM 선택 (기본 90)
   ↓
[녹음] 메트로놈 카운트인(4박) → 가상 피아노로 멜로디 연주
   ↓
[보정] 퀀타이즈 미리듣기 → 적용 (or 원본 유지)
   ↓
[AI 마법] "반주 만들기" → 키 검출 결과 표시 → 드럼+코드 트랙 생성
   ↓
[편집] 드럼 시퀀서로 박자 커스텀, 스타일 프리셋 변경, 볼륨 조절
   ↓
[완성] 재생 → AI 격려 코멘트 → WAV 내보내기
```

---

## 8. 화면 구성 (v0.3.2 갱신 — 모바일 우선)

1. **스튜디오 화면 (모바일 뷰포트 기준 설계, 데스크톱은 반응형 확장)**
   - 상단 바: 재생/정지/녹음, BPM, 메트로놈 토글, 키 표시 (좁은 화면에서는 축약 표시)
   - 중앙: 멀티트랙 타임라인 (모바일에서는 세로 스크롤 + 확대/축소 제스처)
   - 하단: 가상 악기 (피아노 건반 ↔ 드럼 패드 전환), **터치 우선** — 피아노는 가로 스크롤 가능한 건반, 드럼 패드는 큰 정사각형 그리드
   - AI 기능 패널: 데스크톱은 우측 고정, 모바일은 하단 시트/탭으로 전환
2. **드럼 시퀀서 모달**: 16스텝 × 킥/스네어/하이햇/탐 (모바일에서는 스텝을 2줄로 접어 가로 스크롤 최소화)
3. **퀀타이즈 다이얼로그**: 강도·그리드 선택 + 미리듣기
4. **결과 화면**: 재생 + AI 코멘트 카드 + 내보내기 버튼
5. **커뮤니티 위젯 (v0.3.8 신설, 전 화면 공통 오버레이)**: 우상단 공지사항(🔔)·건의하기(✏️) 아이콘, 우하단 전체 채팅(💬) 플로팅 버튼 — 로그인 상태면 프로젝트 목록/스튜디오 어디서든 고정 노출

---

## 9. 기술 스택 및 아키텍처 (v0.3 전면 갱신)

### 9.1 아키텍처 원칙: "소리는 브라우저, 저장은 서버"

- **오디오 관련 전부(연주, 시퀀싱, 반주 생성, 재생, WAV 렌더링)는 프론트엔드에서 처리** — Web Audio는 브라우저에서만 동작하며, 서버로 오디오를 보내면 지연 때문에 연주감이 죽음
- **Django 백엔드의 역할은 3가지로 한정**: ① 프로젝트 저장/불러오기 API (소유자 연결) ② **사용자 계정 (MVP 필수 — 곡의 제작자 식별이 요구사항)** ③ LLM API 중계(키를 서버에 숨김 — 보안상 올바른 구조)
- Django에서 음악 생성 로직을 돌리지 않는다 (역할 침범 금지)

### 9.2 스택 표

| 영역 | 기술 | 비고 |
|---|---|---|
| 프론트엔드 | **React (JavaScript)** | TypeScript 제외 결정. 대신 미디 노트 등 핵심 데이터 구조는 주석/JSDoc으로 형태를 문서화해 실수 방지 |
| 오디오 엔진 | Web Audio API + Tone.js | Transport로 시퀀싱 (변경 없음) |
| 음색 | 샘플 기반 악기 (Tone.Sampler) — 피아노(Salamander), 기타/바이올린/색소폰(tonejs-instruments, CC BY 3.0, 출처 표기 필요) | 신스 음색 금지 원칙 유지. 드럼은 타악기 특성상 신스 사용(원래 표준) |
| 키 검출 | Krumhansl-Schmuckler 구현 (JS, 프론트) | Phase 0 검증 대상 |
| 반주 생성 | 코드: 규칙 기반(프론트 JS) · 드럼: **Magenta.js DrumsRNN (v0.3.7 적용)** | 코드는 다이어토닉 진행 유지(정확성), 드럼은 TF.js로 브라우저 내 생성 — 매번 다른 패턴 |
| **백엔드** | **Django + Django REST Framework** | 프로젝트 CRUD API, LLM 중계. DRF로 JSON API 구성 |
| **인증** (v0.3.1 확정) | Django 내장 인증 + 세션 또는 토큰(DRF) | 이메일/비밀번호 가입·로그인. 소셜 로그인은 2차. **프로젝트는 반드시 소유 사용자에 귀속**, 본인 프로젝트만 조회/수정 가능 |
| **데이터베이스** | **PostgreSQL** | 프로젝트 데이터(미디 노트, 트랙 구성, BPM, 키)를 JSONB 컬럼으로 저장하면 스키마가 단순해짐 |
| 오디오 내보내기 | OfflineAudioContext → WAV (프론트) | 변경 없음. 서버 업로드 아님 |
| AI 코멘트 | 프론트 → Django 중계 → **LLaMA 3.1 8B (Ollama 로컬, v0.3.7 구현 완료)** | 클라우드 API 키 불필요, 완전 로컬 추론 |
| 배포 | 프론트: Vercel/Netlify 무료 · 백엔드+DB: Render/Railway 등 무료 티어 또는 소액 | v0.2까지의 "완전 무료"에서 서버 비용 변수 추가 |

### 9.3 스택 변경의 트레이드오프 (정직하게 기록)

| | 얻는 것 | 잃는 것 |
|---|---|---|
| TS 제거 | 초기 학습·작성 속도 | 미디 데이터 구조 실수를 런타임에야 발견 → JSDoc으로 부분 보완 |
| Django+PG 도입 | 저장 문제 해결, 계정·공유 확장 경로, LLM 키 보안, 풀스택 포트폴리오 | 배포 대상 3개로 증가, 서버 무료 티어 슬립(첫 요청 지연) 등 관리 부담, **일정 +2~3주** |

---

## 10. 개발 로드맵 (v0.2 — Phase 0 신설)

| 단계 | 목표 | 주요 작업 |
|---|---|---|---|
| **Phase 0: 심장 검증** ⭐ | **반주 엔진이 들을 만한지 UI 없이 확인** | 하드코딩 멜로디(미디 배열) → 키 검출 → 규칙 기반 코드+드럼 생성 → Tone.js 재생. **4장의 성공 기준으로 블라인드 테스트** |
| ↳ 테스트 셋 | **"예쁜" 멜로디 3개 + "못난" 멜로디 3개** 필수 구성. 못난 멜로디 조건: (a) 음역이 5도 이내로 좁은 것 (b) 비다이어토닉 노트가 섞인 것 (c) 같은 리듬만 반복되는 단조로운 것. 엔진 판정은 못난 입력에서의 결과를 포함해 내린다 — **좋은 입력만으로 통과 판정 금지** | | |
| ↳ 판정 | 통과 → Phase 1 진행 / 실패 → 음색 교체·패턴 개선 반복, 2주 내 미통과 시 접근법 재설계(Magenta 조기 도입 검토) | | |
| Phase 1: 악기 | 소리 나는 사이트 | 가상 피아노+드럼패드, 메트로놈, 카운트인 녹음 |
| Phase 2: 편집 | 미니 DAW | 타임라인, 블록 편집, 퀀타이즈(미리듣기 포함) |
| Phase 3: AI 통합 | 검증된 엔진을 UI에 연결 | Phase 0 엔진 연동, 드럼 시퀀서, 스타일 프리셋 |
| **Phase 3.5: 백엔드** (v0.3 신설) | 저장 인프라 + 계정 | Django+DRF 프로젝트 CRUD API, PostgreSQL 스키마(JSONB), **회원가입/로그인/권한(본인 곡만 접근)**, 유저끼리 협업하며 만들기 가능, 다른 유저가 들을수 있게 배포 가능, 프론트 연동, 배포 파이프라인 |
| **Phase 4: 마감** ✅ (v0.3.9 완료) | MVP 완성 | **WAV 내보내기**(`Tone.Offline`, v0.3.9) · AI 코멘트(LLaMA 3.1 8B, v0.3.7) · 저장/불러오기 UI(v0.3.5) — 전 항목 완료 |
| Phase 5 (2차) | 확장 | 허밍 입력, Magenta, AI 보컬, 모바일, 곡 공유, 소셜 로그인 |

**MVP 정의 (v0.3.1 갱신)**: Phase 0~4 완료 = "**로그인한 사용자가** 메트로놈에 맞춰 가상 피아노로 멜로디를 치면, 검증된 AI 엔진이 반주를 붙이고, 박자를 직접 고치고, **내 계정에 곡을 저장하고**, WAV로 내보낼 수 있다"


### 10.1 우선순위(P)–일정(Phase) 대응 관계 (v0.2.1 명시)

P번호는 **기능의 중요도**, Phase번호는 **만드는 순서**로 서로 다른 축이다. 대응은 다음과 같다:

| 우선순위 | 구현 시점 |
|---|---|
| P0 (AI 반주) | Phase 0(엔진 검증) + Phase 3(UI 통합) |
| P1 (악기·편집) | Phase 1 + Phase 2 |
| P3 (AI 평가) | Phase 4 |
| P2 (허밍) · P4 (보컬, 모바일, 공유 등 2차) | Phase 5 |

> 핵심인 P0을 가장 먼저 검증하기 위해 구현 순서가 우선순위 번호와 일치하지 않음. 문서 내 "모바일은 P4" = "일정상 Phase 5"로 동일한 대상을 가리킨다.

---

## 11. 리스크 및 대응 (v0.2 갱신)

| 리스크 | 대응 |
|---|---|
| 규칙 기반 반주가 "미디 벨소리" 수준 | **Phase 0에서 선검증** (매몰비용 방지). 샘플 음색 사용 원칙, 실패 시 Magenta 조기 도입 |
| 템포 추정 실패 | 메트로놈 필수 설계로 문제 자체를 제거 |
| 퀀타이즈가 의도와 다른 노트로 스냅 | 강도 조절 + 미리듣기 + 취소 플로우 |
| 키 오검출 | 검출 키 표시 + 수동 변경 허용 |
| WAV 내보내기 난이도 과소평가 | 별도 작업 항목으로 분리, Phase 4 초반에 착수 |
| **TS 제거로 미디 데이터 구조 버그** (v0.3 신규) | 핵심 데이터 구조(노트, 트랙, 프로젝트)를 JSDoc으로 문서화, 저장/불러오기 경계에서 유효성 검사 함수 필수 |
| **백엔드 범위 팽창** (v0.3 신규) | Django 역할을 "저장 + LLM 중계"로 못박음(9.1장). 계정·공유 욕심은 2차로 |
| **무료 티어 서버 슬립/DB 용량 한도** (v0.3 신규) | 개인 프로젝트 규모에선 허용, 배포 플랫폼 무료 티어 조건을 Phase 3.5 착수 시 확인 |
| **모바일 Web Audio 레이턴시/제약** (v0.3.2 신규) | iOS Safari는 무음 모드 시 재생 차단, 첫 터치 제스처 전 AudioContext 잠김 등 데스크톱과 다른 제약 있음. 실기기(iOS Safari, Android Chrome) 조기 테스트 필수, 퀀타이즈로 타이밍 오차 보정 |
| **터치 다중입력(화음) 정확도** (v0.3.2 신규) | 손가락 여러 개로 동시에 건반 누르는 멀티터치 이벤트 처리가 마우스보다 까다로움(touch 이벤트 개별 추적 필요). Phase 1에서 멀티터치 화음 연주를 명시적 테스트 항목으로 포함 |
| 범위 팽창 | MVP = Phase 0~4 고정, 데스크톱 지원은 후순위로 유지하되 완전 배제하지 않음 |

---

## 12. 미결정 사항

- [ ] 서비스 이름
- [ ] AI 보컬 도입 방식 (2차 목표 시점에 결정)

> ✅ **사용자 계정: MVP 필수로 확정** (v0.3.1 — "곡의 제작자를 알아야 한다"는 요구사항. 이메일/비밀번호 기본, 소셜 로그인은 2차) · ✅ 프로젝트 저장 방식: PostgreSQL 확정 · ✅ AI 평가 기준: C안 확정 · ✅ **퀀타이즈 전 원본 타이밍: 보존으로 확정** (v0.3.4 — Phase 2에서 결정. 트랙마다 `originalNotes`를 녹음 시점 그대로 보관, 퀀타이즈는 `notes`에만 적용하고 언제든 원본으로 되돌릴 수 있게 함)
